#include<stdio.h>
#define MAX_USER 3
struct User{
int id;
char name[100];
int balance;
};
int main(){
struct User users[MAX_USER ];
for(int i=0;i<MAX_USER;i++){

    printf("enter the id:" );
    scanf("%d",&users[i].id);
}
for(int i=0;i<MAX_USER;i++){
   printf("%d\n",users[i].id);
}
}
for(int i=0; i<MAX_USER; i++);
print("UserID; %d\n",user[i].id);
printf("Balance :%d\n",user[i].balance);
}
int find;
printf("enter the userid to check the balance;");
scanf("%d",&find);
for(inti=0;i<MAX_USER; i++)



